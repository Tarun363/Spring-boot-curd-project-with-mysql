# Spring-boot-curd-project with mySql database.
# If you want to connect your local base, modify the properties in `application.properties` with yours.
# Update and run the application as spring boot application. 
